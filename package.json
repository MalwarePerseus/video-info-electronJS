{
  "name": "video-info-app",
  "version": "1.0.0",
  "description": "Just a Basic ElectronJS App which uses ffmpeg library to find the info about the videos.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "MalwarePerseus",
  "license": "ISC",
  "dependencies": {
    "electron": "^9.0.0",
    "fluent-ffmpeg": "^2.1.2"
  }
}
